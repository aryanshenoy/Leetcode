class Solution(object):
    def strStr(self, haystack, needle):
        """
        :type haystack: str
        :type needle: str
        :rtype: int
        """
        x=100
        if needle in haystack:
            x=haystack.find(needle)
        else:
            return -1
        if x==100:
            return -1
        return x
        
